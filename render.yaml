services:
  - type: web
    name: scaler-zoom-backend
    env: python
    repo: https://github.com/jayyx3/Scaler-Assignment.git
    branch: main
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
